import type { ReactElement } from "react";

/** Step-01 placeholder proving the React mount lifecycle. Replaced by the real graph view later. */
export function HelloGraph(): ReactElement {
	return <div className="neighborhood-graph-hello">hello graph</div>;
}
