# Ticket: Dev-vault fixture — use a recognizable thumbnail image

**Status:** OPEN
**Origin:** step-05 human smoke run (2026-07-20), QA_CHECKLIST §1 thumbnail item.

## Context

`note1.md` embeds `pic.png`, a synthetic placeholder generated by
`setup:dev-vault`. In the graph its node thumbnail is unrecognizable, so the
human could not actually judge whether the thumbnail feature "answers what is
this note at a glance" — the checklist item went unverified for lack of a
meaningful image.

## Scope

- Bring a **small, recognizable, open-source / public-domain** image into source
  control (human OK'd committing a small asset) and have `setup:dev-vault` copy
  it in as `note1`'s embedded image instead of the generated placeholder.
- Keep it tiny (dev fixture, not shipped with the plugin).
- Re-verify the thumbnail render manually: fixed-height cropped cover, appears
  only once the node is large enough, no spurious "+N" image badge for a single
  image (QA_CHECKLIST §1).

## References

- `.ai_out/step-05-rich-rendering/main/QA_CHECKLIST.md` §1 (thumbnail item)
- dev-vault setup script (`npm run setup:dev-vault`)
