# IMPLEMENTATION_WITH_SELF_PLAN — PRIVATE memory

Task: width-only node real-estate iteration. Snug capped width + wrap-to-2-lines;
remove folder-prefix breadcrumb entirely. Height UNTOUCHED. No commits (top-level owns).

## Plan (all done)
1. constants.ts: char width 8→7; add NODE_MAX_LABEL_WIDTH_PX=200; drop breadcrumb param from estimateNodeLabelWidthPx.
2. graphIdentity.ts: delete breadcrumbFolderOf + VAULT_ROOT_FOLDER; nodeDimensionsPx(node) = max(sizePx, min(CAP, snug)).
3. flowMapping.ts: drop breadcrumbFolder field/threading; toFlowNodeData(node, mainPinned).
4. elkMapping.ts: nodeDimensionsPx(node) (drop breadcrumbFolderOf).
5. NoteNode.tsx: remove breadcrumb span; doc comment.
6. graph-view.css: remove .vicinity-graph-node__breadcrumb rule; update design-intent comment.
7. folderGrouping.ts: 2 stale breadcrumb comments updated.
8. high-level-plan.md:59 Sizing line updated.
9. Tests: graphIdentity.test.ts rewritten; flowMapping.test.ts removed breadcrumb width test + breadcrumb-titles describe.

## Constants chosen + WHY
- NODE_TITLE_CHAR_WIDTH_PX = 7 (was 8). Snug: the 2-line clamp is now the overflow
  safety net, so we no longer overshoot single-line width. ~12–13px font, 7px avg glyph is safe.
- NODE_MAX_LABEL_WIDTH_PX = 200 (new). Roughly balanced vs 160px engine max height →
  readable, not-too-wide box. Past it, title wraps to line 2.
- NODE_LABEL_HORIZONTAL_PADDING_PX = 20 (unchanged).

## Formula
width = max(node.sizePx, min(NODE_MAX_LABEL_WIDTH_PX, ceil(title.length*7)+20))
height = node.sizePx  (UNCHANGED)

## Exact edits (file:symbol)
- src/view/constants.ts:23-47 — constant block + estimateNodeLabelWidthPx(title).
- src/view/graphIdentity.ts — import line; removed breadcrumbFolderOf+VAULT_ROOT_FOLDER; nodeDimensionsPx(node).
- src/view/flowMapping.ts — import (drop breadcrumbFolderOf); FlowNodeData drops breadcrumbFolder?;
  noteNodes map (line ~174); toFlowNodeData signature+body; withGroupDimensions doc comment.
- src/view/elkMapping.ts — import; leafById map (~line 35).
- src/view/NoteNode.tsx — title block (removed conditional span); header doc comment.
- src/view/graph-view.css — removed breadcrumb rule (~line 149); design-intent comment (~65).
- src/view/folderGrouping.ts — 2 comments (lines 7, 24).
- docs-internal/plan/high-level-plan.md:59 — Sizing bullet.
- src/view/graphIdentity.test.ts — full rewrite (5 BDD cases).
- src/view/flowMapping.test.ts — removed 1 test (breadcrumb width reserve) + 1 describe (breadcrumb titles, 4 tests).

## Test data verification
- short "a" sizePx160 → 160.
- MEDIUM_TITLE 15 chars → 15*7+20=125 (between 40 and 200).
- LONG_TITLE 54 chars → 398 → capped to 200.

## Results
- npm test: 55 files / 673 tests PASS (.tmp/impl-test.log)
- npm run check (tsc strict): PASS (.tmp/impl-check.log)
- npm run build: PASS; styles.css regenerated, no breadcrumb rule (.tmp/impl-build.log)

## Notes
- main.js / styles.css regenerated by build (artifacts) — will show as modified; do not hand-edit.
- Historical CHANGELOG / step-05 / ticket docs still mention breadcrumb — left as historical record (not rewritten).
- No engine/height changes. flowMapping.ts <-> elkMapping.ts still both route width through nodeDimensionsPx.
