# EXPLORATION_PUBLIC__e2e — edge-routing__06 e2e harness

## 1. `e2e/edgeRoutingEval.e2e.ts` (197 lines, full read)

Header (`:8-21`) declares it an **eval/measurement** spec for edge-routing__03, explicitly *not* a regression gate (that is `e2e/edgeRouting.e2e.ts`). Serial mode (`:23`).

**Setup**
- `:25` `EDGE_PATH_SELECTOR = ".vicinity-graph-flow .react-flow__edge-path"`; `:26` `BOUNCE_PATH = "note2.md"`.
- `:28-29` `OUT_DIR = <repo>/.out` (gitignored, `.gitignore:17`).
- `:31` **`type LayoutMode = "force" | "layered" | "radial"`** — STALE.
- `:33-41` `PerfEntry { kind: "routing"|"layout", data: {durationMs, nodeCount?, obstacleCount?, edgeCount?} }`.
- `:49-69` `onConsole` scrapes browser `console.debug` lines by substring `"edge routing pass"` / `"elk+d3 layout pass"` and `jsonValue()`s `msg.args()[1]` (the payload object). **Everything in that payload is available**, including `maxDetourRatio` / `meanDetourRatio` — the spec's `PerfEntry.data` type just doesn't declare them (`:36-40`), so they are silently dropped.
- `:71-78` `beforeAll`: `ObsidianHarness.launch()`, attach console listener, `openGraphView()`, `setAllEdgesVisibility()`, `mkdir .out`.
- `:84-90` `setAllEdgesVisibility` writes `edgeVisibility: "all-edges"` via `app.plugins.plugins[<id>].pluginDataStore.saveGlobalView`.
- `:98-114` `renderFixture(mode, centralPath)`: `harness.setLayoutMode(mode)` → reset `pendingPerf` → open `note2.md` (bounce) → open central → wait for first edge path attached → **fixed `page.waitForTimeout(4500)`** (`:111`, deliberate, documented) → collect entries.
- `:117-137` `lastDurations(entries)`: picks the **heaviest** pass of each kind (routing by `obstacleCount`, layout by `nodeCount`) to dodge the bounce-note pass masking the dense pass. Returns only `{routingMs, layoutMs, obstacleCount, edgeCount}`.
- `:139-141` `screenshot(name)` → `.out/edge-routing-<name>.png` (locator `.vicinity-graph-flow`).

**Fixtures driven** (`:143-147`, generated by `scripts/setup-dev-vault.sh`):

| label | central note | scale (measured, edge-routing__04) |
|---|---|---|
| sparse | `note1.md` | 13 obstacles / 10 edges |
| medium | `hub-medium.md` (5x 3-member folder groups + ring) | 21 / 20 |
| dense | `zzdense-hub.md` (~110 ungrouped spokes + chords, capped at 100 nodes) | 101 / 292 |

**What is printed today** — `:153-155`, exactly:
```
[eval] force/${label}: routingMs=${routingMs} layoutMs=${layoutMs} obstacles=${obstacleCount} edges=${edgeCount}
```
Plus `:165` (`[eval] layered/dense: …` same shape), `:177` (`[eval] radial/dense: routingMs=… (gated off) layoutMs=…`), `:190-192` (`[eval] PERF dense/force: …` same 4 metrics).

**`maxDetourRatio` / `meanDetourRatio` are NOT computed and NOT printed anywhere in this spec.** They are computed in the plugin, not the spec: `src/view/GraphViewController.ts:279` `const detour = detourStats(clippedRoutes)` → logged at `:280-286` inside `console.debug("vicinity-graph: edge routing pass", { obstacleCount, edgeCount, durationMs, maxDetourRatio, meanDetourRatio })`. `detourStats` at `GraphViewController.ts:415`, `DetourStats` type at `:401`; pure `detourRatio` in `src/view/edgeGeometry.ts`. Surfacing them in the `[eval]` line = add the two fields to `PerfEntry["data"]` (`:36-40`), thread through `lastDurations` (`:117-137`), append to the `console.log` template (`:154`). No plugin change needed.

**PERF BUDGET gate** — `:183-196`, the one committed assertion of record:
- `:188` re-renders force/dense; `:193` `expect(routingMs).toBeGreaterThanOrEqual(0)`; `:194` `expect(layoutMs).toBeGreaterThan(0)`; `:195` `expect(routingMs).toBeLessThan(layoutMs)`.
- Comment `:184-187` still describes the radial gate — stale prose to fix.
- Historical false-pass hazard: edge-routing__04 Round 1 showed this gate reading a 3-obstacle stale intermediate; fixed by moving clip+detour+log *before* the `isStale` return (`GraphViewController.ts:274-286`) and by `lastDurations` picking the heaviest pass. Trust `obstacles=101` in the printed line as proof the real dense pass was measured.

**Every removed-`layered`/`radial` reference in this file**
- `:31` `type LayoutMode = "force" | "layered" | "radial"`
- `:99` `harness.setLayoutMode(mode)` (writes a field the engine no longer reads)
- `:161-169` whole test `"layered layout routes the dense fixture and captures a screenshot"` (incl. `:162`, `:165` log, `:167` `screenshot("layered-dense")`)
- `:171-181` whole test `"radial layout SKIPS routing (gated) and still renders the dense fixture"` — **the routing gate that no longer exists**; `:180` `expect(routingMs).toBeUndefined()` would now FAIL, since routing is unconditional (`GraphViewController.ts:235` "The routing pass always runs"; ticket 02 removed the edgeRouting setting)
- `:186-187` PERF BUDGET comment referencing radial gating + layered

## 2. Chore ticket `_tickets/e2e-remove-layeredradial-layout-mode-references-left-by-force-layout-only-ticket.md`

`id nid_6lxaenl4oamjxqj6f0eh6rr4c_e`, type chore, priority 3, status open, tags `[e2e, layout, cleanup]`.
- Cause (`:15`): force-layout-only ticket `nid_ihlfchb69wt1hqot6iqy7a9m9_e` (closed) removed the modes but not the e2e suite; the suite still writes a now-ineffective persisted `layoutMode`.
- Scope (`:17-19`), exactly two files:
  - `e2e/obsidianHarness.ts`: `setLayoutMode("layered"|"radial"|"force")` — **collapse to force-only or remove**.
  - `e2e/edgeRoutingEval.e2e.ts`: the `LayoutMode` type, the layered test, the radial-gated test. "Radial routing-gating no longer exists (routing is always on after the edge-routing setting removal)". **Keep only the force-fixture screenshots + the PERF BUDGET test.**
- Context (`:21`): ticket 02 already removed `setEdgeRouting` from the harness and rewrote `e2e/edgeRouting.e2e.ts`; this ticket is residual layout-mode cleanup only.
- Acceptance (`:23`): `npx tsc -p e2e/tsconfig.json --noEmit` passes; **no layered/radial references remain in `e2e/`**; `npm run test:e2e` green.
- Ticket 06 adds one more item to step 0 (`edge-routing06…md:89`): make `maxDetourRatio`/`meanDetourRatio` actually PRINTED in the `[eval] force/<label>` line.

## 3. Full grep of `e2e/` for `layered` / `radial` / removed settings

Command: `grep -rn -iE "layered|radial|layoutMode|setEdgeRouting" e2e/`

**Must change (behaviorally dead / now-wrong):**
- `e2e/edgeRoutingEval.e2e.ts:31, :99, :161, :162, :165, :167, :171, :172, :175, :177, :178, :180, :186, :187`
- `e2e/obsidianHarness.ts:297` (doc comment "Switches the layout mode…"), `:298` signature `setLayoutMode(layoutMode: "layered" | "radial" | "force")`, `:303` `saveGlobalView({..., layoutMode: mode})`, `:305` arg passing

**Benign — do NOT "fix" (unrelated word senses):**
- `e2e/edgeRouting.e2e.ts:22` — "…default `walked-from-center` shows just the radial star, which has no crossings" (describes edge topology, not the layout mode)
- `e2e/obsidianHarness.ts:85` and `:127` — "fixtures layered on top of…" (English usage)

No `setEdgeRouting` / `edgeRouting` setting references remain anywhere in `e2e/`. No other spec (`vicinityGraph`, `controlsRestart`, `pinnedCentralScenario`, `settingsResetReview`, `settingsResetVerify`, `settingsUxVisual`) touches `layoutMode`.

Corroborating: `layoutMode` is now stripped by the persistence parser — `src/persistence/persistedShapes.test.ts:45-50` ("WHEN globalView carries a removed layoutMode field THEN it is ignored"), so `setLayoutMode` is a genuine no-op.

## 4. How e2e is set up and run

- `package.json:14` → `"test:e2e": "bash scripts/run-e2e.sh"`. Filter args pass through: `npm run test:e2e -- edgeRoutingEval.e2e.ts`.
- `scripts/run-e2e.sh` does, in order:
  1. If `OBSIDIAN_PATH` unset → `OBSIDIAN_PATH=$(bash scripts/setup-obsidian-bin.sh)`.
  2. If `OBSIDIAN_E2E_EXTRA_ARGS` unset **and** no `$DISPLAY`/`$WAYLAND_DISPLAY` → exports `OBSIDIAN_E2E_EXTRA_ARGS="--ozone-platform=headless --disable-gpu"`.
  3. `npm run setup:dev-vault` (seeds `.dev-vault` fixtures idempotently **and runs `npm run build`**, copying `main.js`/`manifest.json`/`styles.css` into `.dev-vault/.obsidian/plugins/vicinity-graph/`; build log → `.tmp/setup-dev-vault-build.log`).
  4. `npx tsc -p e2e/tsconfig.json` (root `tsconfig.json:16` sets `"noEmit": true`, inherited).
  5. `npx playwright test --config e2e/playwright.config.ts "$@"`.
- `scripts/setup-obsidian-bin.sh`: **pinned `OBSIDIAN_VERSION="1.12.7"`**, downloads the GitHub release **tar.gz**, caches at `.tmp/obsidian/obsidian-1.12.7/obsidian` (~200MB extracted), prints path on stdout. Also `npm run setup:obsidian`.
- `e2e/playwright.config.ts`: `testDir "."`, `testMatch **/*.e2e.ts`, timeout **120s/test**, expect timeout 15s, `workers: 1`, `fullyParallel: false`, `retries: 0`, reporter `list`, `outputDir: ../.tmp/e2e-artifacts`.
- `e2e/obsidianHarness.ts`: spawns Obsidian with `--user-data-dir=.tmp/e2e/obsidian-config`, `--remote-debugging-port=0`, `--no-sandbox` on Linux, plus `OBSIDIAN_E2E_EXTRA_ARGS` split on spaces (`:158-167`); attaches via `chromium.connectOverCDP` on the stderr "DevTools listening on ws://…" line (`:382-407`). Vault = throwaway copy `.tmp/e2e/vault` of `.dev-vault` (`:52-54`, `:336-354`). Window state pre-seeded 1280x800 (`:78-79`, `:376-379`). Plugin enabled via `app.plugins.setEnable(true)` (`:440-457`).
- **Env vars**: `OBSIDIAN_PATH` (optional on Linux), `OBSIDIAN_E2E_EXTRA_ARGS` (optional). README lines 176-215 document this.
- **Prerequisites**: `node_modules` present; `.dev-vault` absent in this checkout but auto-seeded by `run-e2e.sh`.
- **Runtime**: prior record — `npm run test:e2e -- edgeRoutingEval.e2e.ts` → **6 passed (29.2s)**. Add ~1-2 min for first-run Obsidian download + production build.
- **Screenshots to `.out/`**: yes — `edgeRoutingEval.e2e.ts:139-141` writes `.out/edge-routing-<name>.png`; `beforeAll` mkdirs `.out` (`:77`). `.out/` is gitignored.

**Can e2e run in THIS environment? Yes — evidence, not guess:**
- Linux x86_64 in Docker (`/.dockerenv` present), 32 CPUs, 319G free.
- No `DISPLAY`/`WAYLAND_DISPLAY` → `run-e2e.sh` auto-applies headless Ozone flags.
- Pinned asset reachable: `curl -I -L …/v1.12.7/obsidian-1.12.7.tar.gz` → **HTTP 200**.
- `@playwright/test ^1.61.1` installed; harness CDP-attaches to Obsidian's own Electron/Chromium (no browser download).
- Prior agents in this same repo/environment ran the eval headless and recorded numbers (`.ai_out/edge-routing-04/edge-routing-04-boundary-pins/VERIFICATION__PUBLIC.md`).

## 5. The `.out/public` vault and the `clear-goals.md` screenshot smoke

- Lives at `<repo>/.out/public` — a **real personal vault**, 457MB, 2960 `.md` files. Gitignored.
- Target notes: `.out/public/_/clear-goals.md`; Epictetus counterpart `.out/public/p/Epictetus/Epictetus.md`.
- **Cannot be driven by the e2e harness today**: `ObsidianHarness` hardcodes `DEV_VAULT_DIR = <repo>/.dev-vault` (`e2e/obsidianHarness.ts:52`) with no override, and `prepareVaultCopy` would `rm -rf` + full-copy the target (`:344-345`) — 457MB and destructive-adjacent.
- Therefore the ticket-06 `.out/public` screenshot smoke is a **manual step** today (build → install into `.out/public/.obsidian/plugins/vicinity-graph/` → open in real Obsidian → screenshot). Automating it needs a vault-path override in the harness (out of chore-ticket scope).

## 6. Overriding `EDGE_ROUTING_SHAPE_BUFFER_PX` for the sweep — the mechanism that actually exists

- Definition: `src/view/edgeRouting.ts:71` `export const EDGE_ROUTING_SHAPE_BUFFER_PX = EDGE_PAIR_CURVATURE_PX / 2;` (=17; `EDGE_PAIR_CURVATURE_PX = 34` in `src/view/edgeGeometry.ts`). Applied once per pass at `src/view/edgeRouting.ts:374`.
- **There is no env var, no build-time `define`, no runtime setting.** Verified: `grep -rn "process.env|import.meta.env" src/` → zero hits; `esbuild.config.mjs` has no `define` block.
- The **only existing mechanism is edit-the-constant-and-rebuild** (as the ticket prescribes). Loop per value V:
  1. Edit `src/view/edgeRouting.ts:71`.
  2. `npm run test:e2e -- edgeRoutingEval.e2e.ts` (rebuilds + recopies into `.dev-vault` → `.tmp/e2e/vault`).
  3. Record `[eval] force/{sparse,medium,dense}` lines; copy `.out/edge-routing-force-*.png` aside per value (filenames are fixed).
- **Gotcha**: `npm run build` runs `npm run check` (tsc only). The `=== 17` and `> EDGE_ARROWHEAD_INSET_MIN_PX` assertions are **vitest** (`src/view/edgeRouting.test.ts:109-131`), so `npm run build`/`test:e2e` still succeed with a swept value — only `npm test` goes red during the sweep. Expected; must NOT be "fixed" by loosening those tests.
- The "non-facing attachment count" column is **not** produced by e2e — it came from standalone node probes against libavoid (uncommitted, `.tmp/probe*.mjs`). e2e supplies routing ms / layout ms / detour ratios / screenshots.

## Recommended repair shape for `edgeRoutingEval.e2e.ts`

1. Delete `:31` `LayoutMode`; change `renderFixture(centralPath)` to drop the mode param and `harness.setLayoutMode` (`:98-99`).
2. Delete tests `:161-169` and `:171-181` and their screenshots.
3. Rewrite the PERF BUDGET comment `:184-187`; keep `:193-195` assertions.
4. Extend `PerfEntry["data"]` (`:36-40`) with `maxDetourRatio?: number; meanDetourRatio?: number`, return them from `lastDurations` (`:117-137`, off the same heaviest routing entry), append to `[eval] force/<label>` and `[eval] PERF dense/force` templates (`:154`, `:191`).
5. `e2e/obsidianHarness.ts:297-307`: remove `setLayoutMode` entirely (no remaining caller).
6. Verify: `npx tsc -p e2e/tsconfig.json --noEmit`, then `npm run test:e2e -- edgeRoutingEval.e2e.ts` (expect 4 tests; check dense line shows `obstacles=101 edges=292`).
