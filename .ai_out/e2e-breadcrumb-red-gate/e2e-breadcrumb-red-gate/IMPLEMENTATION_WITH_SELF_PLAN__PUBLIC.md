# IMPLEMENTATION_WITH_SELF_PLAN — PUBLIC

Ticket `nid_yccejkvl0ccqc77olsgg5deka_e` — `npm run test:e2e` red gate. **Gate is green.**

## Decision: the TEST was wrong. No `src/` behavior change.

This **contradicts the exploration hand-off**, which concluded "specified but never built →
implement it in `src/`". Both exploration agents looked only at current `src/` plus the
historical step-05 doc; neither checked git history. The breadcrumb was not *never built* —
it was **built, then deliberately removed**.

### Evidence

`git log -S'breadcrumbFolderOf'` → commit **`998fdac`**, 2026-07-23:
> `feat(node-real-estate): snug capped node width + **remove folder prefix**`
> "Removed the grayed folder breadcrumb entirely (render, CSS, reserved width, threading)."

That commit deleted, in one coherent change:
- the `<span className="vicinity-graph-node__breadcrumb">` render in `NoteNode.tsx`
- `FlowNodeData.breadcrumbFolder` + threading through `flowMapping.ts` / `elkMapping.ts`
- `breadcrumbFolderOf()` in `graphIdentity.ts` **and its unit tests**
- the `.vicinity-graph-node__breadcrumb` CSS rule and its reserved width
- **`docs-internal/plan/high-level-plan.md:59`** — the design source of truth — rewritten to
  the snug-capped-width model (width hugs the title, wraps at `NODE_MAX_LABEL_WIDTH_PX`)

`grep -n breadcrumb docs-internal/plan/high-level-plan.md` → **0 hits today.** *(Corrected in
iteration 1: the plan doc is SILENT about the breadcrumb, it does not document the absence. The
stronger, accurate claim is that `998fdac` **deleted** the plan doc's breadcrumb sentence at
`high-level-plan.md:59` — an affirmative removal from the design source of truth.)* The only surviving mentions are historical: the superseded
`plan/steps/step-05-rich-rendering.md` and old tickets — and `998fdac`'s own PUBLIC file states
it left historical docs as record and updated "only the authoritative `high-level-plan.md`".

So the instruction's escape hatch applies exactly: **the plan documents contradict the test.**
`npm test` never caught it because the unit tests were deleted with the feature; the e2e file was
missed because e2e is not part of `npm test`.

### Why the prior ticket's "do NOT weaken the test" does not bind

`docs-internal/tickets/ticket-e2e-gamma-breadcrumb-fails-headless.md:41-42` said
"Do NOT weaken or delete the test — singleton-folder breadcrumb is covered behavior." Written on
a false premise: its own leads guessed at viewport culling and said `breadcrumbFolderOf` was
"moved during the node width floored change" — it was **deleted**. Ticket now closed with this
root cause recorded.

### The trimming half — already correct, kept

`ObsidianLinkProvider.ts:301-318` `frontmatterTitleOf()` does `value.trim()`, already unit-covered
by `ObsidianLinkProvider.test.ts:286` ("WHEN the title has surrounding whitespace THEN it is
trimmed"). That is the genuinely valuable half of the gamma test and was **preserved**.

## Changes

| File | Change |
|---|---|
| `e2e/vicinityGraph.e2e.ts` | Gamma test keeps the trimmed-title assertion, drops the `solo/` prefix. The vacuous `toHaveCount(0)` note1 test → vault-wide guard that the folder prefix stays removed. Section comment. Rationale in-code at both sites. |
| `docs-internal/plan/steps/step-05-rich-rendering.md` | Marked the two breadcrumb bullets SUPERSEDED (pointing at `998fdac` + high-level-plan) — this doc is what misled both exploration passes. |
| `scripts/setup-dev-vault.sh` | Two stale "breadcrumb" references: the printed human smoke-test checklist (an actively wrong instruction) and the gamma fixture body comment. |
| `src/adapters/ObsidianLinkProvider.ts` | Comment only — "titles and breadcrumbs" → "node titles". No behavior change. |
| `docs-internal/tickets/ticket-e2e-gamma-breadcrumb-fails-headless.md` | Closed with root cause. |

**Tests added: none, deliberately.** Nothing in `src/` changed. A unit test asserting a deleted
symbol's absence would be theatre; the vault-wide e2e guard is the honest regression net.

## Results (verbatim final lines)

`npm test` → exit **0**
```
 Test Files  74 passed (74)
      Tests  990 passed (990)
```

`npm run check` → exit **0** (tsc strict, `src/` then `e2e/`; no output)

`npm run build` → exit **0**
```
copied plugin artifacts to .dev-vault/.obsidian/plugins/vicinity-graph
```
`main.js` / `styles.css` regenerated byte-identical (not shown as modified by git).

**`npm run test:e2e` → exit 0** (`E2E_EXIT=0`, `.tmp/impl-e2e.log`)
```
  1 skipped
  78 passed (52.0s)
```
```
  ✓  63 e2e/vicinityGraph.e2e.ts:96:1 › no node renders a folder-prefix breadcrumb (2ms)
  ✓  73 e2e/vicinityGraph.e2e.ts:178:1 › singleton-folder note shows its trimmed frontmatter title (1ms)
```

### Did anything not run? — No.
**0 failed, 0 "did not run".** The 6 previously-skipped serial tests all ran and passed. The single
`skipped` is `e2e/externalVault.e2e.ts:53` ("WHEN VICINITY_E2E_VAULT points at a vault THEN its
graph renders and is screenshotted") — an intentional env-gated skip, unset by design in a normal
run, **not** a serial-mode casualty. No other spec file was red.

## Rejected approaches

- **Implementing the breadcrumb** (the hand-off's expected path). Rejected: it would re-add a
  feature a human-scoped change deliberately deleted 3 days earlier, and contradict the current
  `high-level-plan.md` sizing model. Reviving it is a product decision, not a gate fix.
- **Deleting the note1 breadcrumb test outright.** Rejected: replaced with a vault-wide guard so the
  removal is actually pinned rather than silently unasserted — the old test's real sin was passing
  vacuously.
- **Loosening the gamma assertion to a substring / regex** to force green. Rejected as hacky.

## Open risks / callouts

- **Someone may still want the breadcrumb.** Its removal was justified by node real-estate, not by a
  judgment that folder identity is worthless — ungrouped non-root notes now show no folder context
  at all. That is a live UX question, but a *product* one; flag to the human if folder identity on
  ungrouped nodes matters. Not re-opened here.
- The gamma fixture body edit only affects **freshly created** vaults (`write_if_missing` never
  rewrites); existing `.dev-vault` copies keep the old sentence until recreated. Comment text only —
  no assertion depends on it.
- `docs-internal/tickets/ticket-step-05-human-smoke-run.md` and the CHANGELOG still describe the
  breadcrumb. Left as historical record per `998fdac`'s stated convention.
