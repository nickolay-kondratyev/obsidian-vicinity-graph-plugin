# IMPLEMENTATION_REVIEW — Edge arrowhead + "×N" badge visual polish (PUBLIC)

Reviewed HEAD `c3b46150`. Context/contract in EXPLORATION_PUBLIC.md +
CLARIFICATION__PUBLIC.md + ticket (not duplicated).

## Verdict: READY

No blockers, no majors. The change is a tight, well-scoped, theme-var-driven visual
tuning that preserves every automated-test contract. Remaining items are human taste
calls on a real render + running e2e in a display-capable env — both already flagged by
the implementer, neither a defect.

## Test results (run in this env)
| Command | Result |
|---|---|
| `npm run check` (tsc -noEmit) | PASS (exit 0) |
| `npm test` (main vitest + sublib) | PASS — 451/451 main, 69/69 sublib (520 total) |
| `npm run test:e2e` | NOT run — no Obsidian binary / no display here. Preserved by construction; MUST run pre-release in a display-capable env. |

## Contract preservation (EXPLORATION §3) — each item confirmed
| Contract item | Status | Evidence |
|---|---|---|
| Arrowhead renders as `<polyline>` in `.react-flow__arrowhead`; `stroke/fill: var(--text-faint) !important` intact | CONFIRMED | graph-view.css:44-47 untouched; markerEnd has no color prop (NeighborhoodGraphFlow.tsx:138-139), so RF still stamps the polyline marker |
| Every edge path keeps `marker-end: url(...)` | CONFIRMED | Only marker SIZE changed; marker still attached |
| Badge keeps `×N` text + `data-count` | CONFIRMED | NeighborhoodEdge.tsx untouched; badgeText.test green |
| `edgeGeometry.test.ts` holds at curvature 34 (interpolates symbol, not hard-coded 24) | CONFIRMED | Test lines 18 & 23 use `${EDGE_PAIR_CURVATURE_PX}` / `/2`; literal asserts are curvature-independent, no path-shortening added; tests green. Implementer's "no edit needed" claim verified TRUE |
| Theme-var / zero own colors | CONFIRMED | grep for hex/rgb in graph-view.css → only `#b1b1b7` inside a comment |
| Folder-group `+N` badge unchanged | CONFIRMED | Split preserved all 7 props of `.neighborhood-graph-group__badge` (graph-view.css:264-272) |

## Findings
| Severity | File:line | Issue | Suggested fix |
|---|---|---|---|
| NIT | NeighborhoodGraphFlow.tsx:32-36; edgeGeometry.ts:19-23; graph-view.css:279 | Comments embed a volatile "2026-07-20 smoke run" date. The stable WHY (quarter-viewBox empty margin; incoming head sits on returning edge) is excellent; the date is arguably volatile per CLAUDE.md doc guidance. | Optional. The `[[ticket-...]]` wiki-link already carries traceability; keeping the date is defensible. No change required. |
| NIT | *.tsx / *.css comments | `[[ticket-edge-arrowhead-and-badge-visual-polish]]` wiki-links live in source (non-markdown) comments, where Obsidian won't resolve them. Harmless textual pointer. | None required; consistent-enough convention. |
| INFO | `styles.css` | On-disk shipped CSS is a stale prior build (old shared badge rule). It is gitignored (build artifact) and regenerated by `npm run build`. | Ensure a fresh `npm run build` runs before packaging the release (standard flow). Not a source issue. |

Note on DRY: the split leaves a few identical declarations (font-size, padding, color)
duplicated across the two badge blocks. This was an EXPLICIT, aligned decision
(CLARIFICATION #3 — split to restyle independently) as the badges diverge visually;
acceptable SRP/OCP trade, not a DRY violation to flag.

## #QUESTION_FOR_HUMAN (taste — needs real render, I concur with implementer)
1. Confirm size 24 / curvature 34 read well on a LIVE Obsidian render, both themes. Proxy
   (`.out/edge-badge-polish/edge-badge-{dark,light}.png`) is faithful for marker geometry +
   CSS but is NOT the live view (no elk layout, approximated hues) — weight accordingly.
2. On the real render, eyeball two things the proxy can't guarantee: (a) the larger head not
   overlapping the node border, (b) the two mirror-pair badges/heads staying individually
   legible near the shared node.
3. If heads still read faint, next lever per implementer is size 24→28 and/or node-gap
   path-shortening — the latter WOULD require updating the literal-string asserts in
   `edgeGeometry.test.ts`. Decide only after a real-render look.

## Release gate
- Automated contract: GREEN in this env.
- Before release: run `npm run test:e2e` in a display-capable env; get human sign-off on the
  taste calls above.
