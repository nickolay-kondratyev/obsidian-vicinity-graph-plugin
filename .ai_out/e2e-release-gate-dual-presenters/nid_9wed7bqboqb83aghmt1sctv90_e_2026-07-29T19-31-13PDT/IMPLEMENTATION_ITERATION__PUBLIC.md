# IMPLEMENTATION_ITERATION — round 1 — PUBLIC

Ticket `nid_9wed7bqboqb83aghmt1sctv90_e`. Responds to `IMPLEMENTATION_REVIEW__PUBLIC.md`
(APPROVE-WITH-FIXES, 0 BLOCKING, 3 SHOULD-FIX). Scope given to me: items **1 and 2**.
Commit `2f14e62` on branch `nid_9wed7bqboqb83aghmt1sctv90_e_2026-07-29T19-31-13PDT`.

## Per-finding decisions

### SHOULD-FIX 1 — sub-pixel flake in the clip comparison — **INCORPORATED**

`e2e/settingsUxVisual.e2e.ts` now filters on a named tolerance instead of an exact
comparison:

```ts
const SECTION_CLIP_TOLERANCE_PX = 1;
…
.filter((section) => section.neededPx - section.shownPx > tolerancePx)
```

The constant is a module-level const with a WHY docblock (independent integer rounding
of `clientHeight` / `scrollHeight`; the gate is documented as runnable on macOS/Windows
via `OBSIDIAN_PATH`), and it is passed into `evaluate` as its second argument rather
than duplicated inside the browser closure. The failure message now interpolates the
tolerance, so a red says by what margin the section is cut.

**Non-vacuity re-verified after the change**, exactly as requested: reverted the CSS
rule to `flex-shrink: 1`, re-seeded the dev vault, ran only that spec — it FAILED
naming 5 sections (`Depth (all notes)` 41/92, `Node sizing` 151/336, `Node contents`
50/113, `Force layout` 101/226, …), i.e. 51-185px of real damage, far outside a 1px
tolerance. Then restored the rule from a backup and re-seeded. Log: `.tmp/it1_red.log`.

### SHOULD-FIX 2 — the WHY comments invert their own meaning — **INCORPORATED**

Both sites rewritten so the fix is never mistakable for the defect.

- `src/view/graph-view.css` — the block above `.vicinity-graph-toolbar__body > *` now
  opens with `DO NOT REMOVE`, then states WHAT BREAKS (the *default* `flex-shrink: 1`
  is the wrong value here: shrinkable children absorb the 60vh overflow, the body's
  scrollHeight never exceeds its clientHeight, no scrollbar appears, and each
  `overflow: hidden` disclosure crops its own rows with no error state — measured, a
  336px Node sizing rendered as 138px), then WHY `0` and that it is a no-op when the
  content fits, and names the guarding test.
- `e2e/settingsUxVisual.e2e.ts` docblock — the sentence *"That is exactly what
  regressed once (`flex-shrink: 0` …)"* is gone. It now says the shrink default was a
  **latent defect for as long as the cap existed** (reachable with Node sizing + Force
  layout both open) and that the `flex-shrink: 0` rule is the FIX which this test keeps
  in place.

Also corrects the review's second point: this was never a regression of the
dual-presenter ticket.

### SHOULD-FIX 3 — change log / ticket close / cap-UX follow-up — **OUT OF SCOPE**

Explicitly assigned to TOP_LEVEL_AGENT. I did not touch `change_log` or any ticket.

### Reviewer's optional suggestions — not taken

Neither was in scope and both are argued against in the review itself (nested
"Advanced spacing" adds nothing because the fix is content-independent; the
non-vacuity assert already explains itself). No silent drops beyond these.

## Files changed

- `/home/nickolaykondratyev/git_repos/nickolay-kondratyev_obsidian-vicinity-graph-plugin/e2e/settingsUxVisual.e2e.ts`
  — tolerance constant + docblock + filter + failure message.
- `/home/nickolaykondratyev/git_repos/nickolay-kondratyev_obsidian-vicinity-graph-plugin/src/view/graph-view.css`
  — WHY block rewritten. **The rule itself is byte-identical**; no behavior change.

Diff: 40 insertions, 17 deletions across the two files. No spec assertion weakened, no
skip added, `main.js` / `styles.css` regenerated by the build only.

## Verification — exactly what I ran

| Command | Result |
|---|---|
| `npm run setup:dev-vault` (CSS is compiled into `styles.css`, so the spec needs it) | exit 0 — `.tmp/it1_setup.log`, `.tmp/it1_setup2.log` |
| `npx playwright test --config e2e/playwright.config.ts settingsUxVisual` (whole spec file, real Obsidian 1.12.7 headless) | **17 passed**, exit 0 — `.tmp/it1_uxspec_final.log` |
| same spec with the CSS reverted to `flex-shrink: 1` | **1 failed** (intended RED), exit 1 — `.tmp/it1_red.log` |
| `npm test` | **1139 passed**, exit 0 — `.tmp/it1_unit.log` |
| `npm run check` | exit 0 — `.tmp/it1_check.log` |

Obsidian was driven with `OBSIDIAN_PATH=$PWD/.tmp/obsidian/obsidian-1.12.7/obsidian`
and `OBSIDIAN_E2E_EXTRA_ARGS="--ozone-platform=headless --disable-gpu"`. I ran the one
spec file rather than the full `npm run test:e2e` gate, per the instruction — the full
gate was already green at `95 passed / 1 skipped` in the previous round and my change
touches only this file plus a CSS comment.

## Readiness

**READY.** Both in-scope findings incorporated, RED re-verified after the tolerance
change, all three verification commands green, committed. Remaining work on this ticket
is SHOULD-FIX 3, owned by TOP_LEVEL_AGENT.
