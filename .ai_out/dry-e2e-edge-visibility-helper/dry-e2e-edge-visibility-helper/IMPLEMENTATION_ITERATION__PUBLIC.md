# IMPLEMENTATION_ITERATION — PUBLIC

Ticket `nid_xwfw86nqr8af7eygqod8lh5cp_e`. Branch `dry-e2e-edge-visibility-helper`.
Reviewer verdict was **READY** (0 BLOCKING, 0 SHOULD-FIX, 2 NITs), so this iteration is
**cosmetic only**. No behavior touched.

## Disposition per reviewer item

| # | Item | Disposition |
|---|---|---|
| BLOCKING | none raised | n/a |
| SHOULD-FIX | none raised | n/a |
| NIT 1 | `e2e/obsidianHarness.ts:345-346` WHY-NOT reads as a statement about today's callers; reword as a method PRECONDITION | **INCORPORATED** |
| NIT 2 | Append the two deferred duplication items to existing ticket `nid_g4iae40tww9abtwrexdrvic0y_e` rather than opening a new one | **ACKNOWLEDGED — not mine.** Explicitly out of my scope per instruction; TOP_LEVEL_AGENT owns ticket bookkeeping. I touched no ticket files. |

### NIT 1 — why I agreed

The reviewer is right, and the reason is the one that matters: the old wording
("callers set this BEFORE opening the central file") was a claim about the *current* two call
sites. It is a fact that silently becomes false when a third caller appears — a comment that
rots by itself. Restating the same information as a precondition of the method makes it a
contract the caller must satisfy, which stays true regardless of how many callers exist, and
it names the remedy for the violating case.

`e2e/obsidianHarness.ts`, JSDoc on `setEdgeVisibility` — the WHY-NOT is retained (it explains
the deliberate divergence from `setNodePreviewPreference`), now subordinate to the precondition:

```
 * PRECONDITION: call this BEFORE the graph view renders the central file — this is a
 * store write only. WHY-NOT fan out to open views like {@link setNodePreviewPreference}:
 * the next rebuild already picks the value up, so a caller that sets it against an
 * already-rendered view must trigger an explicit rebuild itself.
```

## Verification

| Gate | Result |
|---|---|
| `npm run check` | **exit 0** (`.tmp/iter-check.log`) — `tsc -noEmit` + `tsc -noEmit -p e2e/tsconfig.json` |
| `npm run test:e2e` | **NOT re-run — deliberately.** |

The change is a comment body inside a JSDoc block. Zero executable tokens changed
(`git diff` on this iteration is 3 deletions / 4 insertions, all inside `/** … */`). Re-running
the real-Obsidian gate could not distinguish pass from fail here. The full gate set
(`check` + `npm test` 990/990 + real e2e 7 passed) was green on the reviewed commit `2abd131`
and was additionally reproduced independently by the reviewer.

I am stating plainly: **I did not run the e2e suite in this iteration.** Do not read the
green `check` above as an e2e claim.

## Convergence state

**CONVERGED.** Reviewer verdict was already READY; the sole in-scope NIT is incorporated and
the sole out-of-scope NIT is routed to TOP_LEVEL_AGENT. Nothing outstanding on my side, no
new issues found, no follow-up work generated by this iteration. Branch committed, working
tree clean.
