---
id: z1l4jwdai5eudx3dd8k17ji5c
title: "Fix linkStrengthFactor JSDoc range drift in SettingsSpec"
desc: "JSDoc documented [0.25, 2] while the spec ships max: 4; rewrote the bound doc truthfully instead of swapping the digit."
created_iso: 2026-07-27T18:26:29Z
type: docs
impact: 1
author: CC_WITH-nickolaykondratyev
tags: [docs, settings]
dirs: [src/engine, docs-internal/tickets]
---

Closes ticket nid_2yygojiqkdi9hp73pgv0w7qfu_e.

The stale prose was not a bare number — it carried a rationale ('above ~2 the stiff springs
overshoot ... the layout stops converging cleanly'), so a 2 -> 4 digit-swap would have
fabricated a justification.

**Archaeology.** 07c4db7 introduced `max: 2` together with that prose; dee64c3 raised it to
4 as a one-line hand-edit and left the comment untouched. 258ec5a's commit message records
'Human-decided (2026-07-24): the shipped spec value is the intended one'.

**Result.** The JSDoc now documents [0.25, 4] and states honestly that `max 4` is a
maintainer-chosen headroom ceiling, NOT a measured stability limit. The one mechanical fact
is kept and verified against src/view/d3ForceRefinement.ts:83-87 — the factor scales d3's
1/min(degree), so a degree-1 leaf's spring strength IS the factor, which is what makes the
`min 0.25` bound (dominance over centerPullStrength.max 0.15) load-bearing. Comment went
10 lines -> 7; raw commit hash and docs-internal path stay out of production source.

**Sweep (AC2).** All bounded entries in src/engine/SettingsSpec.ts audited twice
independently, from scratch each time. linkStrengthFactor was the only prose/spec
contradiction. No drift in README.md, docs-internal/, src/view/ UI strings, types.ts, or
constants.ts (FORCE_LAYOUT_RANGES is a mechanical projection of the spec, test-verified).

**Side fix.** docs-internal/tickets/ticket-settings-baseline-tests-stale-after-spacing-change.md
misattributed the max raise to 22bd5cb (which moved only linkGapPx/collidePaddingPx);
corrected to dee64c3, plus a note on the 258ec5a trailer. Left OPEN — it asks what `4` was
validated against, which remains a human call.

**Verification.** src/ diff provably comment-only; no spec value or test changed.
npm run check exit 0; npm test 1053/1053 across 79 files. Two review rounds, converged at
0 blocking / 0 should-fix.
