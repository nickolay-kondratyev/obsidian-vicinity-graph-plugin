---
id: upil1fho18prrnjz148ykpxqb
title: "e2e: OBSIDIAN_VERSION env knob + a manifest-floor run (npm run test:e2e:floor)"
desc: "The e2e Obsidian build is now overridable per run; a new test:e2e:floor runs the same suite on manifest.minAppVersion (derived, not a second literal). Floor 1.12.4 and pinned 1.12.7 both: 135 passed."
created_iso: 2026-08-04T22:44:25Z
type: chore
impact: 2
author: CC_WITH-nickolaykondratyev
tags: [e2e, testing]
dirs: [scripts, e2e]
---

Ticket `nid_fygwk293msqdumkkorz6gmyrh_e`.

- `scripts/setup-obsidian-bin.sh`: `OBSIDIAN_VERSION="${OBSIDIAN_VERSION:-1.12.7}"` — default run unchanged and reproducible, any published build reachable per run, versions cached side by side under `.tmp/obsidian/`.
- `scripts/obsidian-floor-version.sh` (new) prints `manifest.json` `minAppVersion`; `scripts/run-e2e-floor.sh` (new, `npm run test:e2e:floor`) runs the suite against it. NO second version literal exists — bumping the manifest is the only edit. The floor runner REFUSES (exit 1) when `OBSIDIAN_PATH` is set, since that binary wins and its green would not be a floor green.
- `e2e/obsidianVersionKnob.test.ts` (new, in `npm test`) guards the `:-` default form, the single pinned literal, the executed floor derivation, and the floor runner exporting the knob.
- Docs: README (script table + "Running against another Obsidian version"), CLAUDE.md commands, `docs-internal/notes/e2e-obsidian-docker-setup.md`.

Verified: `npm run check` green, `npm test` 1603 green, `npm run test:e2e:floor` (Obsidian 1.12.4) 135 passed — the declared floor is now a verified fact — and `npm run test:e2e` (1.12.7) 135 passed.

Follow-up `nid_a5jbonflbm3110gsy6puf18ds_e` covers making the two-version run ONE gate (the human ask on the ticket).

Env note: `node_modules` was stale (`@testing-library/react` declared but not installed), so check/test were red before any change here; `npm install` fixed it, no dependency edit.
