---
id: hziud87mutz3tsq7iy8z1kvzt
title: "e2e slider value-readout: self-diagnosing failure; 1.13 verification blocked"
desc: "Enriched the slider readout union-assertion failure with captured DOM so a future Obsidian 1.13 red reads as a locator problem, not a product regression. The ticket's core ask (verify against a real 1.13 build) is BLOCKED: no public 1.13.x release exists."
created_iso: 2026-07-26T17:37:10Z
type: chore
impact: 2
author: CC_WITH-nickolaykondratyev
tags: [e2e, settings, obsidian-version]
dirs: [e2e, scripts]
---

Ticket `nid_zylnmqz76ftecuqpavnnu1byt_e` (stays OPEN — blocked externally).

## Blocked half

No Obsidian 1.13.x build is obtainable: newest public release is 1.12.7; 1.13.3 exists only as a
Catalyst-gated `.asar.gz` (404 without a licence). The npm typings are at 1.13.1 — they ship ahead
of the GA app, which is why the test's inline readout arm was written from a `@deprecated` note that
no shipped build demonstrates. `OBSIDIAN_VERSION` and `minAppVersion` untouched.

Rejected shortcuts (recorded in the ticket so they are not re-attempted): asar-swapping a 1.13
payload into a 1.12 shell; synthesising 1.13 DOM in the test — both would produce a green that
proves nothing about real 1.13.

`setDynamicTooltip()` in `src/view/VicinityGraphSettingTab.ts` STAYS — its removal condition
(`minAppVersion >= 1.13.0`) is unreachable while 1.13 is not GA. No edit needed.

## Delivered half

`e2e/settingsUxVisual.e2e.ts` — the slider readout asserts a locator union (`.tooltip` for <= 1.12.x
OR inline text for the documented 1.13 rendering). It failed as one opaque Playwright timeout that
never said which arm missed. Now the failure is enriched with the row's `.setting-item-control` DOM
and any body-level `.tooltip`, then re-thrown with the original error as `cause`.

Verified: assertion semantics byte-identical to before once the try/catch scaffolding is removed;
failure never swallowed (diagnostic capture is itself total, so it cannot mask the cause); failing
test costs ~+0.1s. Green on 1.12.7 — check exit 0, 990 unit tests, 16 e2e passed. Perturbation check
observed the enriched output firing.

`scripts/setup-obsidian-bin.sh` — warns a future version-bumper that 1.13 will exercise the
unverified inline arm.
